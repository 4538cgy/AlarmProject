<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppBaseTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:statusBarColor">@color/color_3D4188</item>

        <item name="colorPrimary">@color/color_3D4188</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="colorControlNormal">@color/white</item>
    </style>

    <style name="ViewBasetheme" parent="AppBaseTheme">
        <item name="android:background">@drawable/shape_bg_linear_gradient_default</item>
    </style>

    <style name="gradBaseTheme" parent="AppBaseTheme"/>

    <style name="Theme.Transparent" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="timePickerThemeWhite">
        <item name="android:textColorPrimary">@color/white</item> <!-- color for digits -->
        <item name="android:textColor">@color/white</item> <!-- color for colon -->
        <item name="android:colorControlNormal">@color/white</item> <!-- color for (horizontal) delimeters -->
        <item name="android:textSize">10dp</item>
    </style>
</resources>